    <%# TODO
      if opts[:show_editor] && !message.edit_author.blank? && !message.versions.blank?
        html << ' <span class="versions">(' << cf_link_to(t('messages.versions'),
                                                          versions_Path.message_path(thread, message),
                                                          rel: 'nofollow',
                                                          class: 'version-link')

        html << ')</span>'
      end
    %>