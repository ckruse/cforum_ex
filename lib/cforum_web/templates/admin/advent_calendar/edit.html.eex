<%= render "form.html", Map.merge(assigns, %{action: Routes.admin_advent_calendar_path(@conn, :update, @day.date.year, @day), back: Routes.admin_advent_calendar_path(@conn, :index, @day.date.year)}) %>
