<%= form_for @changeset, Routes.admin_forum_path(@conn, :create), [class: "cf-form"], fn f -> %>
  <%= render "form.html", Map.put(assigns, :f, f) %>

  <p class="form-actions">
    <%= primary_submit gettext("save changes") %>
    <%= default_button_link gettext("cancel"), to: Routes.admin_forum_path(@conn, :index) %>
  </p>
<% end %>
