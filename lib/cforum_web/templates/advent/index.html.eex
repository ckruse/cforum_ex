<ul>
  <%= if Helpers.blank?(@years) do %>
    <li class="no-data"><%= gettext("no years found") %></li>
  <% end %>

  <%= for year <- @years do %>
    <li><%= link year, to: Routes.advent_path(@conn, :show, year) %></li>
  <% end %>
</ul>
