<ul>
  <li><%= link gettext("suggest new cite"), to: Routes.cite_path(@conn, :new) %></li>
  <%= if Abilities.may?(@conn, "cite", :index_voting) do %>
    <li><%= link gettext("vote for cites"), to: Routes.cite_path(@conn, :index_voting) %></li>
  <% end %>
</ul>
