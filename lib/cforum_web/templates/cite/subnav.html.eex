<ul>
  <li><%= link gettext("suggest new cite"), to: Path.cite_path(@conn, :new) %></li>
  <%= if Abilities.may?(@conn, "cite", :index_voting) do %>
    <li><%= link gettext("vote for cites"), to: Path.cite_path(@conn, :index_voting) %></li>
  <% end %>
</ul>
